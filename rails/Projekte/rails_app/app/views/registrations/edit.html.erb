<h1>Editing Registration</h1>

<%= render 'form', registration: @registration %>

<%= link_to 'Show', event_registration_path(@event.id, @registration.id) %> |
<%= link_to 'Back', event_registration_path(@event.id) %>
