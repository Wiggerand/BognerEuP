<%= form_for([@customer, @car]) do |f| %>
  <% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :automarke %>
    <%= f.select :vehicle, Car::VEHICLE_OPTIONS, prompt: "Fahrzeugmarke auswählen" %>
  </div>

  <div class="field">
    <%= f.label :kennzeichen %>
    <%= f.text_field :registration, placeholder: "z.B.HH-HH 123" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
